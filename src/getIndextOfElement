static int getIndex(int[]a ){
        int count =0;
        int target = a[a.length-1];
        for(int i=0; i<a.length; i++){
            if(a[i]<target){
               count++; 
            }
        }
        return count;
    }
